<div id="message-<%= message.id %>" class="<%= current_user == message.user ? "sender" : "reciver" %>">
    <strong><%= message.user.username %></strong>
  <small>
    <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
    <p><%= message.content %></p>
</div>
